{% extends 'base.html.twig' %} {% block title %}Hello IndexController!{%
endblock %} {% block body %}
<meta charset="UTF-8" />

<!-- Insérer cette balise "link" après celle de Bootstrap -->
<link rel="stylesheet" href="https://unpkg.com/bootstrap-table@1.16.0/dist/bootstrap-table.min.css">

<!-- Insérer cette balise "script" après celle de Bootstrap -->
<script src="https://unpkg.com/bootstrap-table@1.16.0/dist/bootstrap-table.min.js"></script>

   <div class="container">
      <h1>Liste des joueurs</h1>
      <table class="table table-striped" data-toggle="table" data-pagination="false">
        <thead>
          <tr>
          	<th>Photo</th>
            <th data-sortable="true" data-field="nom">Nom</th>
            <th>Prénom</th>
            <th data-sortable="true">Classement</th>
          </tr>
        </thead>
        <tbody>
        {% if joueurs is defined %}
        {% for joueur in joueurs %}
        {% if categorie == 'tous' %}
          <tr>
          <td><img src="{{ asset('/images/'~joueur.nomPhoto) }}" alt="" width="70"></td>
            <td>{{ joueur.nom }}</td>
            <td>{{ joueur.prenom }}</td>
            <td>{{ joueur.classement }}</td>
          </tr>
         {% elseif categorie == joueur.libelleCat %} 
          <tr>
          <td><img src="{{ asset('/images/'~joueur.nomPhoto) }}" alt="" width="70"></td>
            <td>{{ joueur.nom }}</td>
            <td>{{ joueur.prenom }}</td>
            <td>{{ joueur.classement }}</td>
          </tr>
        {% endif %}
        {% endfor %}
		{% endif %}
        </tbody>
      </table>
    </div>

{% endblock %}